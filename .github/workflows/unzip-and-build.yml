name: Unzip Android Project
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verificar q.zip en la raíz
        run: |
          ls -la
          test -f q.zip || { echo "❌ No está q.zip en la raíz"; exit 1; }

      - name: Unzip q.zip (auto-detect root)
        run: |
          set -euo pipefail
          rm -rf extracted
          unzip -o q.zip -d extracted
          echo "📁 Después de unzip:"
          (cd extracted && find . -maxdepth 3 -print || true)

          ROOT_DIR=$(find extracted -mindepth 1 -maxdepth 1 -type d | head -n1 || true)
          [ -z "${ROOT_DIR:-}" ] && ROOT_DIR="extracted"

          echo "↪️ Proyecto detectado en: $ROOT_DIR"
          shopt -s dotglob
          rsync -a "$ROOT_DIR"/ .
          rm -rf extracted q.zip

          echo "📦 Contenido final en la raíz:"
          ls -la

      - name: Subir proyecto descomprimido
        uses: actions/upload-artifact@v4
        with:
          name: proyecto-descomprimido
          path: .
          if-no-files-found: error
