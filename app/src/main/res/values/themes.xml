<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la app -->
    <style name="Theme.TVAppVLC" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Colores mínimos que pide Material (pueden ser genéricos) -->
        <item name="colorPrimary">@android:color/black</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@android:color/darker_gray</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorSurface">@android:color/black</item>

        <!-- Barras del sistema -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- Edge-to-edge -->
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>

        <!-- Tipografía por defecto (coincide con Inter en /res/font) -->
        <item name="fontFamily">@font/inter_regular</item>
    </style>

</resources>
